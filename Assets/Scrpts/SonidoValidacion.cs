using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SonidoValidacion : MonoBehaviour
{
    public AudioSource clip;
    // Start is called before the first frame update
    void Awake()
    {
        clip.Play();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
